pipeline {
    agent any

    stages {
        stage('Check Python Version') {
            steps {
                sh 'python --version'
            }
        }

        stage('Run Python Script') {
            steps {
                sh 'python python.py'
            }
        }
    }
}
